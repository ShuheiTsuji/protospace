.jumbotron
  .container.text-center
    = image_tag("header_logo.png", alt: "PROTO TYPE - SHARE THE PROTOTYPE", height: "73px")
.container
  .row
  %h2
    - @tags.each do |tag|
      %ul.col-md-4
        = link_to tag.name, tag_path(tag), class: 'btn btn-defaults'

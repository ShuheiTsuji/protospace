.container.user-new{action: ""}
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = devise_error_messages!
    .col-md-8.col-md-offset-2
      %header.row.user-nav.row
        .col-md-12
          %h2 Register
          .user-name_image.row
            .user-name.col-md-10
              = f.text_field :name, autofocus: true, placeholder: "Username"
            .user-image.col-md-2
              #js-new_product_image3
              %button.btn.btn-primary Add image
              = f.file_field :avatar, autofocus: false, id: "js-product_images_attributes_3_image"
          .user-email
            = f.email_field :email, autofocus: false, placeholder: "E-Mail"
          .user-password
            = f.password_field :password, autocomplete: "off", placeholder: "Password"
            = f.password_field :password_confirmation, autocomplete: "off", placeholder: "Password Confirmation"
        .col-md-12
          %h4 Member of
          .user-user-member.row
            .user-name.col-md-12
              = f.text_field :member, autofocus: false, placeholder: "Member"
          %h4 Profile
          .user-user-profile.row
            .user-name.col-md-12
              = f.text_area :profile, autofocus: false, placeholder: "Profile"
          %h4 Works
          .user-name_image.row
            .user-name.col-md-12
              = f.text_area :works, autofocus: false, placeholder: "works"
      .row.text-center.user-btn
        = f.submit :save, class: "btn btn-lg btn-primary btn-block", data: { disable_with: "sending..." }


